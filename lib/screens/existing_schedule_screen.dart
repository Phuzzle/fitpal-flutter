import 'package:flutter/material.dart';

class ExistingScheduleScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Open Existing Schedule'),
      ),
      body: Center(
        child: Text('Existing Schedule Screen - To be implemented'),
      ),
    );
  }
}
