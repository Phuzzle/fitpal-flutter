import 'package:flutter/material.dart';

class ExistingScheduleScreen extends StatelessWidget {
  const ExistingScheduleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Open Existing Schedule'),
      ),
      body: const Center(
        child: Text('Existing Schedule Screen - To be implemented'),
      ),
    );
  }
}
