import 'package:flutter/material.dart';

class NewScheduleScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Create New Schedule'),
      ),
      body: Center(
        child: Text('New Schedule Screen - To be implemented'),
      ),
    );
  }
}
